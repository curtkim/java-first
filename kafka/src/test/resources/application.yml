spring:
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}

app:
  topic:
    foo: foo.t

logging:
  level:
    root: WARN
    org.springframework.web: INFO
    com.memorynotfound: DEBUG